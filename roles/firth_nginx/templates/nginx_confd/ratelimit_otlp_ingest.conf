# {{ ansible_managed }}
# Abuse protection for the unauthenticated OTLP ingestion endpoint.
# Starting point - tune rate/burst against observed traffic once live.
limit_req_zone $binary_remote_addr zone=otlp_ingest:10m rate=5r/s;
